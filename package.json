{
  "name": "sistema-solar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "three": "^0.185.1"
  }
}
